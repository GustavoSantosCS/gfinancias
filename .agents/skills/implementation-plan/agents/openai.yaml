interface:
  display_name: "Implementation Plan"
  short_description: "Create and iteratively validate implementation plans"
  default_prompt: "Use $implementation-plan to create and validate a complete implementation plan for the request."
policy:
  allow_implicit_invocation: false
